# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# --- caches / tooling ---
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
.memsearch/
.claude/

# --- large data (kept local, never committed) ---
raw_ladcp_test/
raw_CTD/
test_data/
clean_ctd/
Data/
WinADCP/
logsheets/
legacy_code/
# station data / legacy / from-hex cache — local only
CRUISE2_raw/
vbIX/
ctd_from_hex/

# golden / reference data at repo root (the small test fixture under
# tests/fixtures/ is committed; the full local archive is not)
/New_golden/

# raw shipboard-ADCP (VmDAS) archive + its ingest cache — local only, never committed
/sADCP/
*.npz

# --- generated outputs ---
figures/
qa_out/
validation_out/
.ladcp_archive.json
# guide/ (PDF quick-guides + generators) is committed; only its cache is not
guide/__pycache__/

# --- archives / binaries ---
*.rar
*.zip
*.mat
!tests/fixtures/**/*.mat

# raw cruise test datasets dropped into the workspace (never committed)
CCC_ladcp_test/
quick_run.log
wetransfer_*/
