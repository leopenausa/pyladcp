# Appendix B · LDEO_IX ↔ pyladcp mapping

!!! note "Draft pending"
    Planned: a table mapping LDEO_IX parameters (`p.*`, `ps.*`) and outputs
    (`dr` struct, `.lad`/`.bot` files) to their pyladcp equivalents (CLI flags,
    `CastParams`, export files).
