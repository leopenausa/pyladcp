# 3 · Installation

!!! note "Draft pending"
    This chapter is planned but not yet written. Outline:

- pip install from GitHub (the README recipe); conda environment alternative.
- Verifying the install with the test suite.
- Linux / macOS / Windows notes (all CI-tested); headless servers (`MPLBACKEND=Agg`).
- Optional extras: `pyladcp[export]` for Excel; CTD_pipeline for `--from-hex`.
