# 8 · Ship-ADCP integration

!!! note "Draft pending"
    This chapter is planned but not yet written. Outline:

- Why the ship's ADCP helps: an absolute upper-ocean constraint.
- Direct VmDAS STA/LTA ingest (`--sadcp`); transducer depth; caching.
- Acquisition-clock desync and `--sadcp-timeoff auto --sadcp-nav`.
- Section plots: `ladcp-sadcp-section`, `--by time|distance`, `--anomaly`.
- The CODAS route for edited/calibrated products (`--sadcp-source codas`);
  see also `docs/SADCP_CODAS.md` in the repository.
