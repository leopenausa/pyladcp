# 2 · Before you process

!!! note "Draft pending"
    This chapter is planned but not yet written. Outline:

- Instrument expectations: dual-head RDI Workhorse (down + up looker), PD0 `*.000` files.
- CTD requirements: a processed 1-s `.cnv` (or raw `.hex` + `.XMLCON` with `--from-hex`).
- Navigation and (optional) ship-ADCP data.
- Accepted data layouts: `MASTER/` + `SLAVE/` subdirs or one flat directory.
- Keeping logsheets at sea (templates in appendix D).
