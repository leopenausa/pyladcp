# 1 · LADCP primer

!!! note "Draft pending"
    This chapter is planned but not yet written. Outline:

- What an LADCP measures: relative velocity profiles from a falling/rising package.
- Why the raw data is not a velocity profile: the unknown package motion.
- The shear method in plain words; the inverse method in plain words.
- The three constraints that make velocity absolute: bottom track, GPS-barotropic,
  ship-ADCP — where each acts in the water column.
- What good data looks like (and the common ways casts go wrong).
- Pointers: Visbeck (2002) for the derivations; Thurnherr (2010) for error behaviour.
