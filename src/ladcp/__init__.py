"""pyladcp — Pythonic LADCP processing + acquisition-quality assessment.

A re-implementation of the LDEO_IX (Visbeck inverse method) lowered-ADCP workflow,
validated against legacy LDEO_IX outputs (MORIA 2025).

New to the code? Start with ``src/ladcp/README.md`` (a reading order through the
pipeline and a module-by-module map). See also ``docs/ARCHITECTURE.md`` (data
contract) and ``docs/ROADMAP.md``.
"""

__version__ = "0.2.0"
